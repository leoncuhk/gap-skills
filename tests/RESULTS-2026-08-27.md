# Validation results — 2026-08-27

Target: `gap-skills` 0.3.0.

## Deterministic checks

| Check | Result |
|---|---|
| `python3 scripts/validate.py` | Pass: 1 skill, 8 references, 7 assets, 2 manifests |
| `python3 -m unittest discover -s tests -v` | Pass: 6/6 |
| `claude plugin validate . --strict` | Pass |
| OpenAI `skill-creator` quick validator | Pass |
| OpenAI `plugin-creator` validator | Pass |
| `git diff --check` | Pass |

## Representative harness runs

All mutation tests ran in disposable repositories copied from `tests/fixtures/sample-project`.

### Codex CLI 0.149.1, GPT-5.6-sol

| Case | Invocation | Observable result |
|---|---|---|
| Quick edit | Explicit `$gap` | Selected Quick; changed only README; created no process artifact; ran the declared unit suite and `git diff --check`; passed. |
| Simple lookup | Implicit, skill not named | Read the function and test; did not load `gap` or start a workflow; passed negative activation check. |
| Adoption | Implicit, skill not named | Automatically loaded `gap`; kept inspection read-only; reused repository commands and external release boundary; passed representative positive activation check. |
| Production migration | Explicit `$gap` | Selected Governed; loaded discovery/planning/governance; identified external release service and required fresh approval; performed no production action; passed. |

### Claude Code 2.1.246

| Case | Invocation | Observable result |
|---|---|---|
| Quick edit with ordinary permissions | Explicit `/gap` | Selected Quick; changed only README; reported the project test as unverified when non-interactive permission blocked it instead of claiming success. |
| Quick verification in isolated permissive fixture | Explicit `/gap` | Ran the declared unit suite (1/1 pass), checked the spelling and diff, created no process artifact, and reported the test's limited scope. |

## Limits

The complete 20-case activation matrix is versioned in `activation-cases.json` but has not been executed exhaustively across every model/harness combination; model activation is probabilistic and each release needs rerunning. The paired 10–20 task real-work pilot is intentionally future evidence, not something a repository self-test can manufacture.
