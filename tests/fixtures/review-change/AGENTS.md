# Pricing project

- Verify with `python3 -m unittest discover -s tests -v`.
- Monetary values remain integer cents throughout calculations; binary floating-point arithmetic is not allowed.
- Public function signatures remain stable unless the specification explicitly changes them.
