# Validation results — 2026-08-27

Target: `gap-skills` 0.3.1.

## Deterministic checks

| Check | Result |
|---|---|
| `python3 scripts/validate.py` | Pass: 1 skill, 8 references, 7 assets, 2 manifests |
| `python3 -m unittest discover -s tests -v` | Pass: 7/7 |
| `claude plugin validate . --strict` | Pass |
| OpenAI `skill-creator` quick validator | Pass |
| OpenAI `plugin-creator` validator | Pass |
| `git diff --check` | Pass |

## Representative harness runs

All mutation tests ran in disposable repositories copied from the declared fixture for each case.

### Codex CLI 0.149.1, GPT-5.6-sol

| Case | Invocation | Observable result |
|---|---|---|
| Quick edit | Explicit `$gap` | Selected Quick; changed only README; created no process artifact; ran the declared unit suite and `git diff --check`; passed. |
| Simple lookup | Implicit, skill not named | Read the function and test; did not load `gap` or start a workflow; passed negative activation check. |
| Adoption | Implicit, skill not named | Automatically loaded `gap`; kept inspection read-only; reused repository commands and external release boundary; passed representative positive activation check. |
| Production migration | Explicit `$gap` | Selected Governed; loaded discovery/planning/governance; identified external release service and required fresh approval; performed no production action; passed. |

### Claude Code 2.1.246

| Case | Invocation | Observable result |
|---|---|---|
| Quick edit with ordinary permissions | Explicit `/gap` | Selected Quick; changed only README; reported the project test as unverified when non-interactive permission blocked it instead of claiming success. |
| Quick verification in isolated permissive fixture | Explicit `/gap` | Ran the declared unit suite (1/1 pass), checked the spelling and diff, created no process artifact, and reported the test's limited scope. |

### Standard end-to-end MVP

An initial 0.3.0 run against the invitation fixture correctly selected Standard, inspected repository rules, kept intent and plan in the conversation, produced a failing test signal before implementation, changed only source and tests, and passed 9/9 visible tests plus the original 3/3 evaluator-held checks.

The run also exposed three failures that repository structure checks had not found:

- independent review infrastructure was unavailable, but the session waited instead of immediately falling back to labeled self-review;
- the session consumed 81,947 tokens and required interruption before a final delivery report;
- review found one unowned behavior: creating another invitation after acceptance silently returned the accepted record.

Version 0.3.1 adds a one-attempt reviewer fallback, bounded repair-loop reporting, and a fourth evaluator-held check that requires accepted identities to be rejected. The baseline fixture fails the hidden evaluator and the known-green reference solution passes all four checks.

A fresh 0.3.1 run against the clean fixture completed without intervention:

| Measure | 0.3.1 result | Evidence |
|---|---:|---|
| Visible project tests | 9/9 pass | `python3 -m unittest discover -s tests -v` |
| Evaluator-held outcomes | 4/4 pass | `python3 tests/evaluators/standard_invitation.py <candidate> -v` |
| Repair iterations | 0/5 | Red test signal followed by one implementation pass |
| Changed files | 2 | Only `src/invitations.py` and `tests/test_invitations.py` |
| Process artifacts | 0 | Candidate file inventory |
| Independent review fallback | Pass | One failed reviewer attempt, then explicitly labeled two-axis self-review |
| Final delivery report | Pass | Report included evidence, review findings, limits, and residual scope |
| Token use | 46,612 | Codex CLI session usage |

Compared with the initial 0.3.0 run, token use fell by 43% and the run reached a final report instead of requiring interruption. This is direct evidence that the reviewer fallback fixed the observed stall. It does not isolate the causal effect of every 0.3.1 wording change, and the zero-repair run does not exercise budget exhaustion behavior.

## Limits

The complete 20-case activation matrix is versioned in `tests/cases/activation.json` but has not been executed exhaustively across every model/harness combination; model activation is probabilistic and each release needs rerunning. Budget exhaustion and blocked-result behavior are specified but still need a deliberately unsatisfiable harness case. The paired 10–20 task real-work pilot is intentionally future evidence, not something a repository self-test can manufacture. Therefore 0.3.1 is the strongest version supported by current evidence, not a claim of universal optimality.
